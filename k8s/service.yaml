apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: devops-tech-test
  labels:
    app: devops-tech-test-app
spec:
  selector:
    app: devops-tech-test-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
  type: ClusterIP
